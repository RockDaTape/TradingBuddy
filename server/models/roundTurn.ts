// types/roundTurn.ts
/**
 * Round turn model represents a complete trade cycle (entry and exit) that may include multiple individual trades.
 * Used for analyzing consolidated trading activity.
 */
import type {RoundTurn} from '@prisma/client'

export type RoundTurnRecord = RoundTurn
